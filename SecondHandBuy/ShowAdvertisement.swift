//
//  ShowAdvertisement.swift
//  SecondHandBuy
//
//  Created by Kithin Yeung on 19-02-16.
//  Copyright © 2016 Kithin en Stephanie. All rights reserved.
//

import UIKit
/*
class ShowAdvertisement: UITableViewController {
    
    
// MARK: Properties
    
    
// MARK: Functions
    
    // Turn those retrieve objects from Parse into rows, that can display on the TableView
    override func tableView(tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        return storeAdvertisement.allAdvertisement.count
    }
    
    // Create a data source for the rows, so the rows know where to retrieve the data from and display it
    override func tableView(tableView: UITableView, cellForRowAtIndexPath indexPath: NSIndexPath) -> UITableViewCell {
        
        // Create an instance of UITableViewCell, with default appereance
        let cell = UITableViewCell(style: .Value1, reuseIdentifier: "UITableViewCell")
        
        // Create an constant to save all the products
        let advertisement = storeAdvertisement.allAdvertisement[indexPath.row]
        
        // Create a cell to put the description of product in it
        cell.textLabel?.text = 
        cell.detailTextLabel?.text = advertisement.nameArray
        cell.imageView?.image = advertisement.pictureArray
        return cell
    }
}
*/